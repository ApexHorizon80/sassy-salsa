'use client';
import { getProductAvailability, Product } from '@/lib/products';
export function ProductGrid({ products, onAdd }: { products: Product[]; onAdd: (index: number) => void }) { return <div className="product-grid">{products.map((p, i) => { const availability = getProductAvailability(p); return <article className="product-card" key={p.slug}><div className={`product-visual product-${p.image}`}><span className="badge">{availability}</span><div className="mini-jar"><div className="mini-label"><small>SASSY</small>{p.name.split(' ')[0].toUpperCase()}</div></div></div><div className="product-info"><h3>{p.name}</h3><p>{p.description}</p><div className="product-meta">{p.heat} · {p.size}</div><div className="product-price"><span>${p.price.toFixed(2)}</span><button className="add-btn" disabled={availability === 'SOLD OUT'} aria-label={`Add ${p.name}`} onClick={() => onAdd(i)}>+</button></div></div></article>; })}</div>; }
