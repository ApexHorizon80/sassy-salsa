'use client';
import { products } from '@/lib/products';
export function ProductGrid({ onAdd }: { onAdd: (index: number) => void }) { return <div className="product-grid">{products.map((p, i) => <article className="product-card" key={p.slug}><div className={`product-visual product-${p.image}`}><span className="badge">AVAILABLE NOW</span><div className="mini-jar"><div className="mini-label"><small>SASSY</small>{p.name.split(' ')[0].toUpperCase()}</div></div></div><div className="product-info"><h3>{p.name}</h3><div className="product-meta">{p.heat} · {p.size}</div><div className="product-price"><span>${p.price}.00</span><button className="add-btn" aria-label={`Add ${p.name}`} onClick={() => onAdd(i)}>+</button></div></div></article>)}</div>; }
