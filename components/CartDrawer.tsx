'use client';
import { Product } from '@/lib/products';
export type CartLine={index:number;quantity:number};
export function CartDrawer({open,lines,products,onClose,onChange,onCheckout}:{open:boolean;lines:CartLine[];products:Product[];onClose:()=>void;onChange:(index:number,delta:number)=>void;onCheckout:()=>void}){const valid=lines.filter(l=>products[l.index]);const total=valid.reduce((s,l)=>s+products[l.index].price*l.quantity,0);const blocked=valid.some(l=>!products[l.index].is_active||(products[l.index].quantity<l.quantity&&!products[l.index].preorder_enabled));return <><div className={`drawer-backdrop ${open?'open':''}`} onClick={onClose}/><aside className={`cart-drawer ${open?'open':''}`} aria-label="Shopping cart" aria-hidden={!open}><div className="drawer-top"><h2>Your cart</h2><button onClick={onClose} aria-label="Close cart">×</button></div><div id="cartItems">{valid.length?valid.map(l=>{const p=products[l.index];const unavailable=!p.is_active||(p.quantity<l.quantity&&!p.preorder_enabled);return <div className="cart-row" key={p.slug}><div><strong>{p.name}</strong><small>{p.heat} · ${p.price.toFixed(2)}</small>{unavailable&&<small className="cart-error">Unavailable in the requested quantity</small>}</div><div className="qty"><button aria-label={`Decrease ${p.name}`} onClick={()=>onChange(l.index,-1)}>−</button><span>{l.quantity}</span><button aria-label={`Increase ${p.name}`} disabled={(!p.preorder_enabled&&l.quantity>=p.quantity)||!p.is_active} onClick={()=>onChange(l.index,1)}>+</button></div></div>;}):<p className="empty-cart">Your cart is ready for some salsa.</p>}</div><div className="cart-bottom"><div><span>Subtotal</span><strong>${total.toFixed(2)}</strong></div><button className="btn btn-primary" disabled={!valid.length||blocked} onClick={onCheckout}>Continue to checkout <span>→</span></button></div></aside></>}
