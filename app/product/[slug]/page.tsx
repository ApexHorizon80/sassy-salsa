import { getProductBySlug, getProductAvailability } from '@/lib/products';
export default async function ProductPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const product = await getProductBySlug(slug); if (!product) return <main><h1>Product not found</h1></main>; const availability = getProductAvailability(product); return <main style={{ padding: '8vw' }}><p className="eyebrow">Sassy Salsa</p><h1>{product.name}</h1><p>{product.description}</p><p>{product.heat} · {product.size}</p><p>${product.price.toFixed(2)}</p><p>{availability}</p><label>Quantity <input type="number" min="1" max={product.quantity} defaultValue="1" disabled={availability === 'SOLD OUT'} /></label><br /><a className="btn btn-primary" href="/">Back to shop <span>→</span></a></main>; }
