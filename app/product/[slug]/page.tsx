import { getProductBySlug, getProductAvailability } from '@/lib/products';
import { notFound } from 'next/navigation';
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const product = await getProductBySlug((await params).slug); return product ? { title: product.name, description: product.description } : { title: 'Product not found' }; }
export default async function ProductPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const product = await getProductBySlug(slug); if (!product) notFound(); const availability = getProductAvailability(product); return <main className="product-detail-page"><p className="eyebrow">Sassy Salsa</p><h1>{product.name}</h1><p>{product.description}</p><p>{product.heat} · {product.size}</p><p>${product.price.toFixed(2)}</p><p className="product-availability">{availability}{product.preorder_enabled && availability === 'SOLD OUT' ? ' · PRE-ORDER AVAILABLE' : ''}</p><label>Quantity <input type="number" min="1" max={Math.max(1,product.quantity)} defaultValue="1" disabled={availability === 'SOLD OUT' && !product.preorder_enabled} /></label><br /><a className="btn btn-primary" href="/shop">Back to shop <span>→</span></a></main>; }
